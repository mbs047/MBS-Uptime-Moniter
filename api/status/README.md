# Status API

This folder contains repository-level API documentation for the status application.

Runtime Laravel route and controller code still lives inside `apps/status/routes/api.php` and `apps/status/app/Http/Controllers/Api`.

Included here:

- `openapi.yaml` for the current public API contract
- `examples/` with request and response payload samples for the public status endpoints
